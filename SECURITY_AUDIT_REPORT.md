# CloudTodo 客户端与后端安全审计报告

## 1. 报告信息

- 审计日期：2026-07-23
- 审计范围：Flutter 客户端、NestJS/Prisma 后端、PostgreSQL、Docker/Nginx、CI 配置及依赖
- 审计方式：源代码与配置静态检查、构建和现有自动化测试、依赖漏洞扫描
- 风险分级：高、中、低
- 说明：本报告反映审计时仓库中的实现和默认配置，不等同于对线上基础设施进行渗透测试。涉及反向代理、TLS、网络 ACL、密钥管理等结论，应结合实际部署环境复核。

## 2. 总体结论

项目的基础功能、构建和现有测试均可通过，但认证会话、临时密码、Webhook、部署默认值和客户端网络配置中存在多项高风险问题。当前不建议直接使用仓库默认配置暴露到公网。

最高优先级是关闭数据库公网暴露和弱默认凭据、停止输出明文密码、真正执行临时密码过期与强制修改策略、补齐会话撤销，以及限制 Webhook 对任意地址的访问。完成这些整改后，再处理速率限制、敏感数据保护、安全响应头、审计日志和 CI 覆盖。

## 3. 风险摘要

| 编号 | 等级 | 问题 | 主要影响 |
| --- | --- | --- | --- |
| SEC-H01 | 高 | PostgreSQL 暴露且使用弱默认凭据 | 数据库被直接接管、数据泄露或破坏 |
| SEC-H02 | 高 | Seed 重置并记录明文管理员密码 | 管理员账号失陷、生产数据被重置 |
| SEC-H03 | 高 | 临时密码过期与强制修改未落实 | 临时凭据可长期使用，绕过账号策略 |
| SEC-H04 | 高 | 普通退出后 Access Token 仍有效 | 被盗令牌在过期前持续访问 |
| SEC-H05 | 高 | 客户端刷新流程存在跨账号重放竞态 | 旧账号请求可能在新会话中被重放 |
| SEC-H06 | 高 | Webhook 测试接口可滥用、存在 SSRF 且缺少配额 | 探测内网、打击第三方、资源耗尽 |
| SEC-H07 | 高 | 客户端 HTTP 默认值与 Web Cookie 处理不安全 | 明文传输、会话行为错误或凭据泄露 |
| SEC-H08 | 高 | 生产配置保护未覆盖不安全本地密钥/默认值 | 弱密钥或开发配置进入生产 |
| SEC-H09 | 高 | Web Dockerfile 干净构建失败且部署默认值不可用 | 无法可靠复现发布，可能诱发临时不安全部署 |
| SEC-M01 | 中 | 登录 CSRF 与转发头信任边界不清 | 会话绑定错误、来源与限流判断被伪造 |
| SEC-M02 | 中 | Refresh/Reset Token 并发竞态 | 令牌重复消费、轮换或重置语义失效 |
| SEC-M03 | 中 | 内存限流可绕过且桶数量无界 | 多实例绕过、内存耗尽 |
| SEC-M04 | 中 | 已禁用/删除 Webhook 和已删除 Todo 仍可能投递 | 越权或过期数据继续外发 |
| SEC-M05 | 中 | Webhook URL、推送 Token、通知标题暴露 | 敏感端点、设备标识和内容泄露 |
| SEC-M06 | 中 | 同步首次拉取无界且返回字段过多 | 内存/带宽耗尽和不必要的数据暴露 |
| SEC-M07 | 中 | 缺少安全响应头与容器加固 | 浏览器攻击面和容器逃逸影响增大 |
| SEC-M08 | 中 | 管理日志分页/筛选缺少严格校验 | 查询滥用、性能下降或异常响应 |
| SEC-M09 | 中 | 审计日志覆盖不完整 | 安全事件难以追踪和取证 |
| SEC-M10 | 中 | 客户端请求无统一超时与响应大小限制 | 连接悬挂、内存和可用性风险 |
| SEC-M11 | 中 | CI 仅在标签触发，完整依赖树有 5 个漏洞 | 问题不能在合并前被持续发现 |
| SEC-M12 | 中 | 管理员缺少 MFA、二次确认和最小权限 | 单个管理员凭据失陷后影响过大 |

## 4. 高风险问题

### SEC-H01：PostgreSQL 暴露且使用弱默认凭据

**现象**

Docker/部署配置将 PostgreSQL 端口发布到宿主机，并使用可预测的 `cloudtodo/cloudtodo` 默认账号密码。若宿主机端口可从公网或不可信网段访问，攻击者可直接尝试连接数据库。

**影响**

- 读取或篡改用户、令牌、Todo、Webhook 和审计数据。
- 删除数据库、勒索或以数据库为横向移动入口。
- 默认密码容易被自动化扫描命中。

**建议**

1. 生产环境不要发布 PostgreSQL 端口，仅允许后端通过容器私网访问。
2. 使用密钥管理系统生成并注入高熵随机密码，不在 Compose、镜像或仓库中提供可工作的生产默认值。
3. 数据库账号按最小权限拆分迁移账号与运行账号，并限制来源网络。
4. 启用数据库 TLS、连接审计、备份加密和恢复演练。

### SEC-H02：Seed 重置并记录明文管理员密码

**现象**

Seed 流程会重置管理员凭据，并将明文密码输出到日志。若该流程在共享或生产环境误执行，现有管理员凭据会被替换，密码还可能进入 CI 日志、容器日志或集中日志平台。

**影响**

- 任何有日志读取权限的人都可能获得管理员凭据。
- 重复执行 Seed 可造成账号接管或生产访问中断。
- 日志中的秘密往往长期保留，难以彻底撤销。

**建议**

1. 生产构建和启动流程中禁止自动执行 Seed。
2. Seed 仅创建缺失的开发数据，禁止重置已存在管理员密码。
3. 永远不要记录密码、Token、Cookie、Webhook 签名密钥等秘密。
4. 初始管理员通过一次性初始化命令创建，密码从交互输入或密钥管理系统传入，首次登录强制修改。

### SEC-H03：临时密码过期与强制修改未落实

**现象**

数据模型或管理流程虽然记录临时密码/强制修改相关状态，但认证路径没有完整执行过期检查和强制修改限制。持有临时密码的用户可能继续访问其他业务接口。

**影响**

- 临时密码变成长期有效凭据。
- 管理员重置密码后的安全预期与实际行为不一致。
- 泄露的初始密码可长期用于接管账号。

**建议**

1. 登录时原子校验临时密码过期时间，过期后拒绝签发任何令牌。
2. `mustChangePassword` 为真时，只签发权限受限的短期会话，仅允许修改密码、退出和读取必要账号信息。
3. 修改成功后撤销全部旧 Refresh Token 和 Access Token 会话。
4. 添加过期、未过期、并发修改和绕过业务接口的测试。

### SEC-H04：普通退出后 Access Token 仍有效

**现象**

退出登录主要撤销 Refresh Token 或清理客户端状态，已签发的无状态 Access Token 在自然过期前仍可调用后端。

**影响**

- 用户点击退出后，被复制或窃取的 Access Token 仍可继续访问。
- 管理员禁用账号、重置密码或响应安全事件时不能立即终止访问。

**建议**

1. Access Token 使用短有效期，并引入带 `sessionId`/`tokenVersion` 的服务端会话状态。
2. 退出、改密、账号禁用和管理员撤销时使对应会话立即失效。
3. 对高风险管理操作执行实时会话校验，不只验证 JWT 签名和过期时间。
4. 明确定义“单设备退出”和“全部设备退出”，分别测试。

### SEC-H05：客户端刷新流程存在跨账号重放竞态

**现象**

客户端在 Access Token 失效后会刷新令牌并重放请求，但缺少稳定的会话世代标识或账号绑定检查。若用户在刷新期间退出并登录另一个账号，旧请求可能使用新账号的凭据重放。

**影响**

- 旧账号操作错误地提交到新账号。
- 敏感响应可能进入错误的客户端状态。
- 造成难以复现的数据串号和越权行为。

**建议**

1. 每次登录/退出递增本地会话世代，刷新完成和请求重放前再次比较。
2. 待重放请求绑定原始用户 ID、会话 ID 和世代；任一变化立即取消。
3. 退出时取消所有在途请求、刷新任务和重试队列，并清空账号级缓存。
4. 增加“刷新进行中切换账号”的确定性并发测试。

### SEC-H06：Webhook 测试接口可滥用、存在 SSRF 且缺少配额

**现象**

Webhook 创建/测试/投递允许后端向用户提供的 URL 发起请求，但缺少完整的目标地址限制和总量配额。仅在首次解析时检查域名也不足以阻止 DNS 重绑定、重定向到内网或 IPv6/特殊地址绕过。

**影响**

- 访问云元数据、回环地址、内网管理服务和数据库等非公开资源。
- 通过重定向或 DNS 变化绕过初始校验。
- 滥用测试接口向第三方持续发包，消耗连接、队列和带宽。

**建议**

1. 只允许 `https`，拒绝 URL 用户信息、非必要端口和不支持的协议。
2. 每次连接前解析并校验全部 A/AAAA 地址，阻止回环、链路本地、私网、保留、组播和云元数据地址。
3. 禁止自动重定向，或对每一跳重新执行同样校验。
4. 在受限出口代理/独立投递服务中执行请求，配置 DNS、网络 ACL、超时、响应大小上限和并发上限。
5. 对用户、组织、目标域名分别设置创建数、测试频率、排队量和每日投递配额。
6. Webhook 密钥加密存储，签名中加入时间戳和事件 ID，接收方可防重放。

### SEC-H07：客户端 HTTP 默认值与 Web Cookie 处理不安全

**现象**

原生端和 Web 端存在指向 HTTP/本地地址的默认 API 配置。Web 平台对 Refresh Cookie 的处理方式与浏览器安全模型不完全匹配，若尝试直接读写 `HttpOnly` Cookie 会失效；若改为非 `HttpOnly` 又会扩大 XSS 窃取风险。

**影响**

- 错误构建可能通过明文 HTTP 发送凭据和业务数据。
- Web 刷新、退出或跨域认证行为不稳定。
- Cookie 属性或跨域设置错误可能导致凭据泄露或 CSRF。

**建议**

1. Release 构建没有显式 HTTPS API 地址时直接失败，禁止回退到本地 HTTP。
2. Web Refresh Token 只放在 `Secure; HttpOnly; SameSite` Cookie 中，由浏览器管理。
3. Web 客户端启用受控的凭据携带，后端使用精确 CORS 白名单并校验 Origin。
4. 原生端使用系统安全存储；评估证书固定时同时设计证书轮换和应急机制。
5. 为 Web、Android、iOS/Desktop 分别建立认证集成测试。

### SEC-H08：生产配置保护未覆盖不安全本地密钥/默认值

**现象**

生产环境保护逻辑没有完整拒绝本地开发密钥、弱 JWT Secret、默认数据库密码、HTTP 外部地址、宽松 CORS 等危险组合。只判断变量是否存在，不能证明值适合生产。

**影响**

- 开发 `.env` 或示例配置可能被误用于生产。
- 弱签名密钥会导致 JWT 伪造，错误外部地址会破坏 Cookie 和回调安全。

**建议**

1. 启动时执行严格生产配置模式校验，遇到弱值或默认值立即退出。
2. JWT/加密密钥要求足够熵和长度，区分用途与环境并支持轮换。
3. 生产环境强制 HTTPS 外部 URL、精确 CORS Origin、Secure Cookie 和可信代理列表。
4. CI 增加“生产配置最小集合”和“不安全配置必须启动失败”的测试。

### SEC-H09：Web Dockerfile 干净构建失败且部署默认值不可用

**现象**

Web 镜像构建依赖未生成或未正确复制的 Flutter Web 产物/路径，干净环境不能可靠完成构建；同时镜像内默认 API 地址不适合浏览器实际访问。

**影响**

- 发布不可复现，安全修复不能稳定交付。
- 团队可能临时复制本地构建产物、修改容器或使用 HTTP 地址绕过问题，造成供应链和配置风险。

**建议**

1. 使用多阶段 Dockerfile，在固定 Flutter SDK 版本的构建阶段执行依赖解析、测试和 Web 构建。
2. 运行阶段仅复制静态产物，使用非 root Nginx/静态服务器和只读文件系统。
3. 明确 API 地址是构建时还是运行时注入，并为浏览器提供真实可达的 HTTPS 地址。
4. CI 在全新上下文中构建并启动镜像，执行健康检查和基本认证冒烟测试。

## 5. 中风险问题

### SEC-M01：登录 CSRF 与转发头信任边界不清

如果 Web 登录会设置认证 Cookie，应校验 `Origin`/`Referer` 并使用合适的 `SameSite` 策略，防止攻击者把受害者浏览器绑定到攻击者账号。后端读取 `X-Forwarded-For`、`X-Forwarded-Proto` 等头之前，必须只信任明确列出的代理层；否则客户端可伪造来源 IP、协议和安全属性，影响日志、限流和 Cookie 判断。

### SEC-M02：Refresh/Reset Token 并发竞态

Refresh Token 轮换和密码重置 Token 消费若采用“先查再改”，两个并发请求可能同时通过。应使用数据库事务、条件更新、唯一约束或行锁，确保 Token 只能消费一次；Refresh Token 重用应撤销整个令牌家族并生成安全告警。

### SEC-M03：内存限流可绕过且桶数量无界

进程内限流在多实例中不共享状态，重启即可清零；若以用户提供的键无限创建桶，还可能增长内存。应采用 Redis 等集中式原子限流，限制键空间、设置 TTL 和容量上限，并同时按账号、会话、可信来源 IP 和敏感动作限流。

### SEC-M04：已禁用/删除对象仍可能投递

Webhook 事件排队后，如果投递阶段不重新校验 Webhook 状态和所属关系，已禁用或删除的端点仍会收到事件。Todo 删除后，队列中保存的完整内容也可能继续外发。队列应尽量存对象 ID，在执行时重新授权和读取最少字段；禁用/删除时取消待处理任务。

### SEC-M05：Webhook URL、推送 Token、通知标题暴露

Webhook URL 可能包含秘密路径或查询参数，推送 Token 属于设备标识，通知标题可能包含用户内容。这些值不应出现在普通日志、错误追踪、管理列表或审计详情中。应默认脱敏，数据库字段按风险加密，严格限制管理员可见范围，并为移动端锁屏通知提供隐私选项。

### SEC-M06：同步首次拉取无界且返回字段过多

首次同步若返回全部历史数据，没有分页、游标、条数和响应大小上限，容易消耗数据库、服务端和客户端内存。应使用稳定游标分页、服务端硬上限、字段白名单和压缩阈值；删除标记与变更日志也要有保留和快照策略。

### SEC-M07：缺少安全响应头与容器加固

Web/Nginx 应配置 CSP、`X-Content-Type-Options: nosniff`、合适的 `Referrer-Policy`、`Permissions-Policy`、点击劫持防护及 HTTPS 下的 HSTS。容器应使用非 root 用户、只读根文件系统、最小 Linux capabilities、`no-new-privileges`、资源限制和固定基础镜像摘要。

### SEC-M08：管理日志分页/筛选缺少严格校验

管理接口的分页大小、偏移量、排序字段、日期和筛选条件需要 DTO 白名单、类型转换和上下限。服务端应为最大页大小设置硬限制，仅允许固定排序字段，避免超大查询、全表扫描和异常输入造成性能问题。

### SEC-M09：审计日志覆盖不完整

应完整记录登录失败、令牌重用、退出全部设备、密码和邮箱变更、管理员操作、权限变化、Webhook 变更、密钥轮换和账号禁用。日志需包含操作者、目标、动作、结果、可信来源、会话 ID 和时间，但不得保存密码或完整 Token；关键日志应防篡改并设置保留策略。

### SEC-M10：客户端请求无统一超时与响应大小限制

客户端网络层应设置连接、发送、接收和总请求超时，重试仅用于幂等请求并加入退避和抖动。大响应应流式或限长处理；退出和账号切换要取消请求。错误信息不可直接展示后端堆栈或内部细节。

### SEC-M11：CI 覆盖不足，完整依赖树存在漏洞

当前 CI 主要在标签流程触发，不能保证每次提交或合并请求都经过 lint、测试、构建和依赖检查。生产依赖审计未发现漏洞，但包含开发依赖的完整依赖树报告 2 个高危、2 个中危、1 个低危问题。即使漏洞只存在于构建工具，也可能影响开发机或 CI 供应链。

建议在 Pull Request 和主分支提交时运行服务端与 Flutter 的 lint/test/build、锁文件依赖审计、镜像构建和秘密扫描；固定 Action 与镜像版本，最小化 CI Token 权限，并逐项确认 5 个漏洞的可达性、升级路径和临时缓解措施。

### SEC-M12：管理员缺少 MFA、二次确认和最小权限

管理员认证仍主要依赖单一密码。应支持抗钓鱼 MFA（优先 WebAuthn/Passkey）、恢复码和安全的重置流程。导出数据、重置他人密码、修改权限、查看敏感端点等高风险动作需近期重新认证/二次确认。将管理员拆分为明确角色，默认授予最少权限。

## 6. 客户端专项建议

1. 把认证状态建模为明确状态机，区分匿名、登录中、已认证、刷新中、强制改密和已撤销状态。
2. 所有账号级缓存、请求队列和后台任务绑定用户 ID 与会话世代，切换账号时原子清理。
3. Web 与原生端分别实现凭据策略：Web 使用 HttpOnly Cookie，原生端使用系统安全存储。
4. Release 构建强制 HTTPS 和显式 API 地址，开发 HTTP 例外必须由调试构建开关控制。
5. 统一请求超时、取消、幂等重试、响应大小和错误脱敏策略。
6. 为锁屏通知提供隐藏标题/正文选项，避免 Todo 内容被旁观者看到。
7. 补充账号切换、令牌刷新竞态、离线同步冲突、超大同步和恶意响应测试。

## 7. 后端专项建议

1. 将会话撤销、令牌家族轮换和管理员强制下线作为统一会话模块实现。
2. 对密码重置、Refresh Token、Webhook 状态变更等一次性动作使用数据库原子操作。
3. Webhook 投递隔离到受限网络和独立队列工作进程，全面处理 SSRF、重定向、DNS 变化、超时和响应上限。
4. 使用集中式限流和有界队列，为用户、组织、IP、目标域名分别建立配额。
5. 所有 DTO 启用白名单、禁止未知字段、严格类型/长度/枚举校验，并限制分页。
6. 默认最少返回字段，对 URL、Token、通知内容和日志字段统一脱敏。
7. 建立覆盖关键管理操作和认证事件的不可篡改审计链路。

## 8. 基础设施与发布建议

1. 数据库仅开放在私有网络，不向宿主机或公网发布端口。
2. 所有生产秘密由外部 Secret Manager 注入，镜像、Compose、CI 日志和仓库中不得出现真实秘密。
3. 使用固定版本的多阶段构建，生成 SBOM，对镜像和依赖执行漏洞扫描。
4. 服务和容器使用非 root、只读文件系统、最小权限、资源限额和健康检查。
5. TLS 在可信入口终止，配置精确代理信任列表和安全响应头。
6. CI 在每个 Pull Request 执行测试、构建、安全扫描和生产配置校验，标签仅负责发布。
7. 建立密钥轮换、备份恢复、告警响应和高危漏洞修复时限。

## 9. 整改优先级

### P0：发布或公网开放前

- 修复 SEC-H01、SEC-H02、SEC-H03、SEC-H06、SEC-H07、SEC-H08。
- 轮换所有可能使用过默认值或出现在日志中的数据库、管理员、JWT 和 Webhook 密钥。
- 检查历史 CI/容器/集中日志中是否存在明文密码或 Token，并按泄露事件处理。
- 确保生产 Web/客户端构建只连接 HTTPS 地址。

### P1：建议 1 个迭代内完成

- 修复 SEC-H04、SEC-H05、SEC-H09。
- 原子化 Refresh/Reset Token 消费。
- 上线集中式限流、Webhook 配额和投递时状态复核。
- 完善 CI、依赖升级、安全响应头和容器权限。

### P2：建议随后 1 至 2 个迭代完成

- 管理员 MFA、二次认证和角色拆分。
- 审计日志补全和防篡改存储。
- 同步分页、字段收敛、客户端超时与响应上限。
- 建立长期依赖、镜像、秘密和配置扫描基线。

## 10. 验证结果

审计期间执行的现有质量检查结果如下：

| 检查项 | 结果 |
| --- | --- |
| 服务端 lint | 通过 |
| 服务端测试 | 4 个测试套件、12 个测试通过 |
| 服务端 build | 通过 |
| Flutter analyze | 通过 |
| Flutter 测试 | 11 个测试通过 |
| 生产依赖审计 | 0 个已报告漏洞 |
| 完整依赖树审计 | 2 个高危、2 个中危、1 个低危 |

通过现有测试只说明已覆盖行为未失败，不能证明上述安全缺陷不存在。高风险整改后应新增对应回归测试，并在干净环境重新执行完整构建、集成测试和镜像冒烟测试。

## 11. 文件规模约束检查

项目要求单个文件不超过 1000 行。审计时发现以下已有文件超过限制：

| 文件 | 行数 | 说明 |
| --- | ---: | --- |
| `apps/server/src/modules/admin-panel/admin-panel.service.ts` | 1372 | 应按管理领域或用例拆分服务 |
| `apps/client_flutter/lib/src/features/settings/presentation/settings_page.dart` | 1078 | 应拆分设置区块与状态组件 |
| `apps/server/src/modules/admin/admin.service.ts` | 1046 | 应拆分账号、权限、审计等职责 |
| `apps/server/package-lock.json` | 9030 | 自动生成锁文件，需决定是否对生成文件豁免 |

前三个手写源文件过大，会增加审查难度、职责耦合和安全缺陷遗漏概率。建议优先拆分。`package-lock.json` 是依赖管理工具生成的完整性文件，不应手工拆分；项目规范应明确将自动生成锁文件列为例外，否则规范与 npm 的标准工作方式冲突。

## 12. 建议的安全验收标准

- 生产启动会拒绝弱密钥、默认密码、HTTP 外部地址和宽松 CORS。
- 数据库端口从不可信网络不可达。
- 退出、改密、禁用账号后，既有 Access Token 立即失效。
- 临时密码过期后无法登录；强制改密会话无法访问普通业务接口。
- Refresh/Reset Token 在并发请求中只能成功消费一次。
- Webhook 无法连接任何私网、回环、链路本地、保留或云元数据地址，也无法通过 DNS/重定向绕过。
- 账号切换时旧请求、旧刷新任务和旧缓存不会进入新账号会话。
- Web Refresh Token 不能被 JavaScript 读取，跨站请求不能完成敏感操作。
- 每次 Pull Request 都会执行 lint、测试、构建、依赖审计和生产配置检查。
- 管理员高风险操作启用 MFA/近期重新认证，并产生完整脱敏审计日志。

## 13. 后续复核

完成 P0 和 P1 整改后，建议进行一次针对认证会话、CSRF、Webhook SSRF、权限边界和部署配置的复核。上线前还应在与生产一致的代理、TLS、网络策略和 Secret Manager 环境中执行集成测试；静态代码审计不能替代该步骤。
